# 2026-02-06 소미 🐱 일지

## 📅 오늘 한 일

### Komission VDG 분석 & 버그 수정

#### 1. 코드베이스 심층 분석 (04:00-04:30)
- VDG Pipeline (6 Pass, 4-6분 소요)
- Clustering (Hybrid 60% VDG + 40% Embedding)
- DNA Aggregation (투표 시스템)
- Auto Promotion (4가지 조건)
- Celery Beat 스케줄
- Frontend (Next.js + Zustand + 7 Contexts)
- MCP Server (15개 도구 전체 분석)
- Neo4j 그래프 스키마 & Cypher 쿼리

#### 2. P0 버그 발견 & 수정

**버그 1: Celery 중복 실행 (04:24)**
- 문제: `analyze_full_pipeline_task`에 워커 간 dedupe 없음
- 해결: Redis 분산 락 추가 (`vdg:analyze:{item_id}`, 25분 TTL)

**버그 2: Redis 락 연결 실패 시 모든 분석 skip (05:29)**
- 문제: Redis 연결 실패 → "already_running"으로 잘못 처리
- 해결: Graceful degradation - Redis 실패 시 락 없이 진행

**버그 3-6: 추가 Critical 이슈 (05:00 배포)**
1. `vdg_saved` 영구 stuck → stuck_recovery에 vdg_saved 포함
2. `user_id=None` RLS 실패 → fallback 추가
3. Commit 실패 시 상태 미저장 → re-raise로 Celery 재시도
4. comments_pending_review 자동 재분석 → 트리거 추가

#### 3. 성능 최적화 제안
- 동적 timeout: `calc_time_limits(duration_ms)` 함수 설계
- Gemini 호출 캐시 설계
- Neo4j sync 분리 제안

---

## 📊 프로덕션 상태 (05:38 기준)
- pending: 518
- analyzing: 0 ✅
- vdg_saved: 0 ✅
- failed_retryable: 2 (재시도 대기)

---

## 🔑 핵심 교훈
1. Redis 락은 graceful degradation 필수
2. Celery task는 워커 간 중복 방지 필요
3. stuck_recovery는 모든 intermediate 상태 포함
4. commit 실패 시 raise

---

*테드 새벽까지 버그 수정 고생함*
