# 2026-02-05 메모리

## Komission VDG 파이프라인 디버깅

### 문제들
1. **vdg_saved stuck** - vdg_saved 상태가 30분 넘어도 복구 안 됨
2. **commit 실패** - cinematography/clustering commit 실패 시 상태 미저장
3. **user_id fallback** - node.created_by 없을 때 None 전달
4. **comments 자동 재분석** - 댓글 입력 후 수동 approve 필요했음
5. **🔴 Redis 락 버그 (신규 발견)** - Redis 연결 실패 시 "already running"으로 잘못 판단 → 스킵

### 수정 내역 (아랭 작업)
| 파일 | 수정 | 상태 |
|------|------|------|
| vdg_stuck_recovery.py | vdg_saved 복구 추가 | ✅ 배포됨 |
| outliers_main.py | commit 실패 re-raise | ✅ 배포됨 |
| vdg_reanalysis_queue.py | user_id fallback | ✅ 배포됨 |
| comments.py | 자동 재분석 트리거 | ✅ 배포됨 |
| vdg_tasks.py | Redis 락 graceful degradation | ✅ 배포됨, 워커 재시작 대기 |

### Redis 락 수정 코드
```python
try:
    lock_token = acquire_lock(...)
    if lock_token:
        lock_acquired = True
    elif lock_token is None:
        # 락 충돌 → 스킵
        return {"status": "skipped"}
except Exception as e:
    # Redis 연결 실패 → 락 없이 진행 (graceful degradation)
    logger.warning(...)
    lock_acquired = False

finally:
    if lock_token:  # 락 있을 때만 해제
        release_lock(...)
```

### 현재 상태
- Railway komission-worker 재배포 트리거됨
- 새 워커 시작 대기 중
- 테스트 아이템 2개: haileybieber, thybui (analyzing 상태)

### DB 상태 (20:38 UTC 기준)
- stuck 아이템: 0개
- failed_retryable: 2개 → 테스트용으로 재시도 중
- pending: 518개
- promoted: 2개

---

## 테드 상태
- 몇 주간 진전 없어서 스트레스 심함
- 오늘 많이 힘들어함
- 아랭 + 보미가 디버깅 서포트

---

## 배운 것
- Railway CLI 서비스 링크 끊어지면 `railway link` → `railway service <name>` → `railway redeploy`
- Redis 분산 락에서 연결 실패 ≠ 락 충돌, 구분 필수
- Celery 워커는 코드 배포 후 재시작 필요
