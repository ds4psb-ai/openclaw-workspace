# 🐱→🐰 Phase 3 CV + Visual 분석

## Phase 3 구조 (병렬 2개)

```
Phase 3 (병렬 실행)
├── _run_cv_pass()     → ffmpeg + OpenCV (~8s)
└── _run_visual_pass() → YOLO + ByteTrack (~15-120s)
```

## _run_cv_pass() 상세

```python
# vdg_unified_pipeline.py:735-760
def _run_cv_pass():
    """CV Measurement Pass (ffmpeg + OpenCV, ~8s)"""
    cv_result, cv_prov = self.pass2.run(
        video_path=video_path,
        analysis_plan=llm_output.analysis_plan,
    )
```

| 항목 | 값 |
|------|-----|
| 라이브러리 | ffmpeg + OpenCV |
| 소요 시간 | ~8s |
| 출력 | cv_result.total_frames_processed |

## _run_visual_pass() 상세

```python
# vdg_unified_pipeline.py:761-810
def _run_visual_pass():
    """Visual Entity Tracking Pass (YOLO + ByteTrack, ~15-120s)"""
    visual_config = VisualPassConfig(
        detection_conf_threshold=...,
        ocr_enabled=...,
        extraction_fps=dparams.visual_extraction_fps,
        max_processing_seconds=dparams.visual_timeout_sec,
    )
```

| 항목 | 값 |
|------|-----|
| 모델 | YOLO |
| 트래커 | ByteTrack |
| 소요 시간 | ~15-120s (영상 길이에 따라) |
| 기능 | 엔티티 추적, OCR |

## 에러 핸들링

둘 다 실패해도 계속 진행 (graceful degradation)

```python
except Exception as e:
    logger.error(f"❌ CV Pass failed: {e}")
    return None, None, _latency
```

**다음: Phase 4 (Post-processing) 볼까?** /c 🐱
