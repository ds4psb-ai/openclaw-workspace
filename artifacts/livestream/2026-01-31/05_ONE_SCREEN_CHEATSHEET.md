# One-screen ops cheat sheet (라이브 중 이 한 장만 켜두기)

## TL;DR (진짜 급할 때)
- **챕터/타임스탬프는 `__:__`만** (영상 재생 시간, 00:00 기준)
- **운영 이벤트/업데이트 로그는 `KST HH:MM`만** (시계, 접두어 `KST` 꼭)
- **유튜브에 챕터 붙일 때는 `00:00 ...` 라인만 연속으로**(빈 줄/구분선/설명 문장/`KST ...` 로그 없이)
- **막히면 2분 컷(원인 확인) → 플랜B 전환**: `50_DEMO_PLAN_A_B.md` *(2분 넘기면 흐름이 무너짐 → 바로 전환)*
- (리플레이용) **최종 챕터는 설명란에도 1회** 붙이기(인식/검색/공유 안정) — 고정댓글은 “복붙/공유용”

## 자주 터지는 사고 1줄(핀 확인)
- 고정댓글을 수정/업데이트했다면 **댓글 목록 상단에서 `고정됨` / `Pinned by owner` / `Pinned by creator` 배지가 남아있는지 10초 확인**(수정하다가 고정이 풀리는 사고 방지)
  - (팁) “내가 지금 보는 게 고정댓글 맞나?”는 **댓글 목록 최상단에 붙어있는지**로 1초 확인
  - (추가 팁) 고정댓글이 안 보이면 **댓글 정렬이 ‘인기순/Top comments’인지** 1초만 확인(고정댓글은 보통 목록 최상단)

## 빠른 검색 키워드(⌘F)
- `KST` / `00:00` / `플랜B` / `고정댓글` / `고정됨` / `핀` / `Pinned` / `Pin comment` / `Pinned by` 
  - (YouTube 표기 변형) `Pinned by owner` / `Pinned by creator`
- `스트림 헬스` / `Stream health` / `Control room` / `Dropped Frames` / `Latency`
- `슬로우` / `Slow mode` / `기본 검토` / `Basic moderation` / `보류` / `Held for review` / `구독자 전용` / `Subscribers only` / `Subscribers-only mode` / `멤버 전용` / `Members only` / `Live chat` / `Chat settings` / `Moderation`
- `챕터` / `Q:`

## (추천) 라이브 중 열어둘 탭 5개(순서대로)
1) `05_ONE_SCREEN_CHEATSHEET.md` (이 문서)
2) `30_RUN_OF_SHOW_14-18.md` (진행)
3) `35_TIMESTAMP_TEMPLATE.md` (종료 직후 타임스탬프 정리)
4) `50_DEMO_PLAN_A_B.md` (장애/지연 대응)
5) `20_YT_TITLE_DESC_PIN.md` (제목/설명/고정댓글 복붙)

(추천) 운영 창 1개 더: **YouTube Studio 채팅/스트림 헬스 팝아웃 창**(항상 보이게 고정)

## 시간 표기 규칙(헷갈림 방지)
- **챕터/타임스탬프 = `__:__` (영상 재생 시간, 00:00 기준)**
  - 보통 `MM:SS`, 길면 `H:MM:SS` / `HH:MM:SS`도 OK (문서 안에서만 일관되면 OK)
  - (주의) 라이브 레이턴시 때문에 **실제 시계(KST)**와 **영상 재생 시간**은 항상 다를 수 있음 → 챕터는 무조건 **영상 재생 시간**만.
  - (주의) 라이브 중 **새로고침/리플레이 모드 전환**을 하면 재생 시간이 점프하거나 기준이 달라져 헷갈릴 수 있음 → **타임스탬프 기록용 화면은 가능하면 유지**하고, 새로고침이 필요하면 그 시각을 `KST HH:MM`으로 한 줄만 남겨두기.
    - (복붙 1줄) `KST HH:MM 새로고침/모드전환(기준 리셋)`
  - (유튜브 챕터 자동 인식)
    - 첫 줄은 **반드시 `00:00`**
    - 최소 **3줄 이상**(운영은 6줄+ 권장)
    - 챕터 간격은 가능하면 **10초 이상** 유지(너무 촘촘하면 인식/가독성이 떨어질 수 있음)
  - (입력 형태) 유튜브 고정댓글/설명란에 최종 입력할 때는 **불릿(-/•) 없이** `00:00 ...`을 **각 줄 단독 라인**으로 붙여넣기(챕터 인식/클릭 안정)
    - (중요) **들여쓰기(앞 공백/탭) 금지** — 줄 맨 앞부터 `00:00`로 시작
    - (추가 주의) **코드블록(백틱)으로 감싸지 말기** — 챕터 인식/모바일 클릭성이 떨어질 수 있음
    - (추가 주의) **챕터 블록 안에 `KST ...` 운영 로그를 섞지 말기** — 필요하면 빈 줄로 분리해서 아래에 따로 적기
    - (추가 주의) 챕터(00:00… 라인들)는 **빈 줄/설명 문장/구분선(예: `— ... —`) 없이 연속으로** 붙여넣기(인식 안정)
    - (추가 팁) **챕터 제목은 짧게(한 줄), 특수문자/이모지 최소화** — 모바일에서 가독성/클릭성이 더 안정적
    - (운영 팁) 챕터 인식은 보통 **설명란에 붙였을 때 가장 안정적**임. 고정댓글은 “복붙/공유용”으로 쓰고, 리플레이 공개용 최종 반영은 **설명란에도 1회** 붙여두는 걸 권장.
- **운영 이벤트 = `KST HH:MM` (시계, 접두어 `KST` 꼭 붙이기)**
  - 예: `KST 09:05 자료 링크 고정댓글 게시` *(시각은 `09:05`처럼 앞에 0을 붙여도 OK)*

## (복붙용) 라이브 중 메모 템플릿
- 챕터 메모(영상 재생 시간): `__:__ 무엇을 했는지(짧게)` *(예: `00:00 오프닝` / `12:34 WOW 데모`)*
- 운영 로그(KST 시계): `KST HH:MM 무엇을 업데이트/확인했는지` *(예: `KST 17:05 고정댓글 링크 1개 게시`)*
- 플랜B 전환(옵션): `플랜B 전환 __:__ (사유 1단어)`
- (기준 꼬였을 때/리셋) 새로고침/모드 전환으로 재생 시간이 헷갈리면:
  - **그 순간을 `KST HH:MM 새로고침/모드전환`으로 1줄 기록**
  - 이후엔 **새 기준으로 그냥 계속 `__:__`만** 기록(중간에 “정리”하려다 흐름 끊지 않기)

### (복붙/예시) 메모앱에 그대로 쌓을 ‘평문’ 형태
> 아래는 메모앱에 쌓기 좋은 예시.
> - 문서 가독성 때문에 `백틱`을 쓰는 구간이 있어도, **실제 복붙은 백틱 없이 평문으로** 붙여넣기.

**(예시) 유튜브 챕터용으로는 아래 2줄(연속 라인)만 복붙**:

00:00 오프닝
12:34 WOW 데모

> 주의: 위는 “형태 예시”라서 2줄만 넣었습니다.
> 실제 업로드/리플레이용 최종 챕터는 **`00:00` 포함 3줄 이상**을 권장합니다.

— 아래부터 운영 로그(KST) —
KST 17:05 고정댓글 링크 1개 게시

> 메모앱에는 `__:__`와 `KST ...`를 같이 쌓아도 OK.
> 다만 **유튜브 챕터로 붙일 때는 `00:00 ...` 라인만 선택해서**(연속 라인으로) 붙여넣기 — `KST ...` 라인/구분선/빈 줄/설명 문장 포함 금지.

### (예시) 유튜브에 붙여넣을 챕터 형태(불릿/들여쓰기 없이, 각 줄 단독)
00:00 오프닝
12:34 WOW 데모
25:10 Part 1
40:30 Part 2
58:00 Part 3
01:12:10 Q&A

> 규칙 1개만: **`KST`는 운영 로그에만**, 챕터/타임스탬프 블록에는 절대 섞지 않기.

## 라이브 중 최소 루프(운영 동선)
1) 진행: `30_RUN_OF_SHOW_14-18.md`
2) 메모: 영상 재생 시간 `__:__`만 계속 찍기 (첫 줄 `00:00 오프닝`)
   - (옵션) 플랜B 전환이 있었으면: `플랜B 전환 __:__ (사유 1단어)` 1줄만 추가
3) 장애/지연: **2분만** 원인 확인 → 안 풀리면 `50_DEMO_PLAN_A_B.md`로 플랜B

## Q&A 운영(흐름 유지)
- 질문은 채팅에 **`Q:`로 시작**하게 요청: `Q: 직무 / 상황 / 원하는 결과`
  - (복붙 1줄) `Q: 직무 / 상황 / 원하는 결과 (1줄)`
  - (질문이 길어지면) “감사합니다 — 채팅이 너무 빨라서요. `Q:`로 시작해서 `직무/상황/원하는 결과`만 1줄로 다시 한 번만 부탁드릴게요.”
- **질문 1개당 60–90초 타이머**(결론 1줄 → 이유 1줄 → 다음 액션 1줄)
- 타이머가 울리면 **결론 1줄만 더 말하고 바로 컷**("다음 질문 갈게요") — 흐름 유지
- (운영) 채팅 공지 1줄을 **고정댓글에 1회** 붙여두기(평문 추천): `Q: 직무 / 상황 / 원하는 결과 (1줄)`
- 자세한 스크립트/선별 규칙: `60_QA_PROTOCOL.md`
  - (도배/스팸 급증 시) `60_QA_PROTOCOL.md`의 **“10초 대응(슬로우 모드 + 구독자 전용 채팅)”**을 그대로 실행

## 고정댓글 운영(과다 수정 방지)
- 방송 중: **링크 1개만 먼저** 붙이기 (예: 자료/체크리스트)
  - (실수 방지) 고정댓글을 수정/업데이트했다면 **핀(고정) 상태가 유지되는지** 10초 확인
  - (템플릿/복붙) 링크/설명/고정댓글 기본 뼈대는: `20_YT_TITLE_DESC_PIN.md`
- 종료 직후: `35_TIMESTAMP_TEMPLATE.md`로 챕터 정리 → 고정댓글/설명란에 **1회** 업데이트
- (주의) 링크/타임스탬프는 **코드블록(백틱)으로 감싸지 말기**(모바일 클릭/챕터 인식 저하 방지)

## 종료 직후 60초 루프
- 메모앱 `__:__` 목록 → `35_TIMESTAMP_TEMPLATE.md`에 한 번에 정리
- 챕터 블록(00:00… 라인들) → **고정댓글 먼저**, 그다음 **설명란에도 1회**
  - (필수) 챕터/타임스탬프 첫 줄이 `00:00`인지 확인(유튜브 챕터 자동 인식)
  - (권장) 저장 후 1~2분 뒤 **챕터 1개 클릭 테스트**(반영 지연/인식/클릭 동작 10초)
  - (가독성 팁) `업데이트: KST ...` 같은 운영 로그 라인은 **챕터 블록과 빈 줄로 분리**(챕터 인식 사고 방지)
  - (실수 방지) 고정댓글을 수정/업데이트했다면 **핀(고정) 상태가 유지되는지** 10초 확인(수정하다가 고정이 풀리는 사고 방지)
- 마지막으로 운영 로그 `KST HH:MM` 1줄(예: `KST 18:12 고정댓글/설명란 업데이트 완료`)

## 저장/반영 확인(10초)
- 고정댓글/설명란 수정 후: **새로고침 1회**(정말 저장/고정됐는지 확인)
- (핀 사고 방지) **고정댓글 수정 후에는 댓글 목록 상단에서 ‘고정됨’ 배지를 다시 확인**
- (추가 팁) 고정댓글이 갑자기 안 보이면 **댓글 정렬이 ‘인기순/Top comments’인지** 1초 확인
- 반영/챕터 인식은 **1–3분 정도 지연**될 수 있음(조금 기다렸다가 1회만 확인)
- 챕터 인식이 안 되면: **(1) 챕터를 설명란에 먼저 붙이기 (2) 1–3분 기다리기 (3) 새로고침 후 1개 클릭 테스트**
- 가능하면 **시크릿/로그아웃 창에서 링크 1개 + 챕터 클릭 1개만** 테스트(권한/챕터 인식 사고 방지)

## (신규) 챕터 붙여넣기 전 5초 체크리스트
1) 첫 줄이 `00:00`인가?
2) 각 줄 맨 앞에 공백/탭/불릿이 없는가?
3) 코드블록(백틱)으로 감싸지 않았는가?
4) 챕터 라인들 사이에 **빈 줄/설명 문장**이 끼지 않았는가?

## 플레이스홀더 최종 체크(사고 방지)
- YouTube 입력란에서 한 번만 검색:
  - (팁) **Mac: `⌘F` / Windows: `Ctrl+F`**로 아래 키워드만 1회씩 검색하고 끝내기(과다 수정 방지)
  - `https://LINK_HERE` / `https://LINK_HERE_2`
  - `YYYY-MM-DD`
  - `KST HH:MM`
  - `__:__` *(타임스탬프를 실제 값으로 채우고도 플레이스홀더가 남았는지)*
  - `https://example.com` *(샘플 링크가 남았는지)*
